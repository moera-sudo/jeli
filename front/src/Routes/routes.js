/**
 * Centralized route path definitions.
 * Keeping paths in one place avoids magic strings across the app and makes
 * redirects easy to reason about.
 */
export const ROUTES = {
  home: '/',
  login: '/login',
  register: '/register',
}
