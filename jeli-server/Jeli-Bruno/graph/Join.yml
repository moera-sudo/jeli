info:
  name: Join
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/graph/join
  body:
    type: json
    data: |-
      {
        "invite_code": "ABCD1234"
      }
  auth:
    type: bearer
    token: PASTE_ACCESS_TOKEN_HERE

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
