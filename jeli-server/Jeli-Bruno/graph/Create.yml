info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/graph/create
  auth:
    type: bearer
    token: PASTE_ACCESS_TOKEN_HERE

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
