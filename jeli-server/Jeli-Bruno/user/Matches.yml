info:
  name: Matches
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/users/{{user_id}}/matches"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
  scripts:
    - type: tests
      code: |
        test("returns a list of match candidates", function () {
          expect(res.body).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
