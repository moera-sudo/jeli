info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/auth/register
  body:
    type: json
    data: |-
      {
        "email": "user@example.com",
        "password": "string123",
        "full_name": "Trofim Bystritskiy",
        "graph_invite_code": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
