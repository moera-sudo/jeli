info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "user@example.com",
        "password": "string123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
